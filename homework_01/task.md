По дороге на лекцию по машинному обучению Миша нашел флешку с брелком, на котором написано "медкомиссия". 
На флешке оказался табличный файл с названием data.csv.
Поскольку целевой переменной в csv файле Миша не обнаружил, он справедливо заключил, что это должна быть задача на кластеризацию.
Однако, информацию про количество кластеров Мише обнаружить нигде не удалось.
Миша не часто ходит на пары в университет, а на практике не слушал преподавателя - поэтому о существовании библиотек sklearn он не в курсе (интернета нет - узнать тоже не может).
Однако краем уха он слышал на лекции о существовании алгоритмов кластеризации и готов пойти в библиотеку, чтобы почитать про их реализацию, чтобы потом написать код самостоятельно (без использовании библиотек).
Помогите Мише понять количество кластеров и правильно кластеризовать данные.

P.S. нельзя использовать именно готовую реализацию алгоритмов кластеризации (по сути все то что импортится из sklearn.cluster) (остальные сопутствующие инструменты можно)
подбор параметров тоже будет необходимо обосновать, как и сам выбранный алгоритм (почему именно такой)